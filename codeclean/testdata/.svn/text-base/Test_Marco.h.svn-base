#define SIZE    2*5
#define LENGTH  1 << 4
#define WIDE    (10+1)
#define HIGH    14-4
#define DEEP    10/2
#define EXP     ((((1+2)*3 -1) >>2 )|3)

struct Marco{
	int size[SIZE];
	char length[LENGTH];
	int  wide[WIDE];
        char high[HIGH];
	char deep[DEEP];
	int  exp[EXP];
};
